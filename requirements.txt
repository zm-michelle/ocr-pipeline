# Core deep learning
torch>=2.0.0
torchvision>=0.15.0

# Image processing
Pillow>=9.0.0
# headless: the GUI build drags in Qt and crashes on display-less machines (HPC
# login nodes); <5 because the weeks-old 5.0 wheels also segfault on RHEL 8.
opencv-python-headless>=4.7.0,<5

# Numerical
numpy>=1.23.0

# Live training curves (python main.py --mode train_* ; tensorboard --logdir runs)
tensorboard>=2.14.0

# Web app (web/server.py)
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9

# No spell correction, no language model — intentionally omitted.
# If you later add beam-search with an LM, add kenlm or pyctcdecode here.
