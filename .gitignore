__pycache__/
*.py[cod]
.DS_Store

# virtualenvs
myenv/
.venv/
venv/

# trained weights
checkpoints/

# OCR results, previews, and smoke-test artifacts
outputs/

# runs/ holds TensorBoard event files + metrics.jsonl per training run
runs/

# credentials — never commit these, wherever they end up
.env
.env.*
config.toml
.runpod/
*.pem
*.key
data/synthetic_docs/
remote/hpc.env

# derived from NoisyOffice via `python main.py --mode pseudo_label ...` (needs tesseract); regenerate, do not commit
data/noisyoffice_labels/
